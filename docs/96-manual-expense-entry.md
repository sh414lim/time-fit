# 카드 외 직접 지출 등록

- 현금, 계좌이체, 임차료처럼 카드 원천이 없는 지출을 확정 원장에 등록한다.
- 거래일, 사용처, 총금액, 분류, 사유와 선택 담당자를 입력한다.
- 공급가액과 부가세는 둘 다 입력하고 합계가 총금액과 같은지 서버와 DB에서 이중 검증한다.
- 날짜·사용처·금액이 같으면 중복 의심으로 차단하고, 관리자가 확인한 경우만 별도 건으로 허용한다.
- 지출·manual 원천·감사 로그를 하나의 DB 함수에서 원자적으로 생성한다.
