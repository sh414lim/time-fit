# 운영 피드백 후속 검토 — 2026-09-20

기준: `docs/100-operations-feedback-design.md`, `docs/101-operations-feedback-scenario-test.md`, 최신 `main` 코드. 이 문서는 새 운영 테스트 결과를 주장하지 않는다.

## 코드 대조 결과

- 홈 확인할 일, 완료 주 매출·메뉴 비교, 근태 유형별 목록·정정, 카드 미검토 목록과 권한 제한, 정정 감사 이력은 이미 구현되어 있다.
- 기존 홈은 해당 유형이 0건이어도 근태·휴가·카드 행을 보여주고, 저장된 급여 초안도 확인할 일로 표시했다. 이번 변경으로 실제 확인 대상이 있는 유형만 표시하고, 대상이 없으면 짧은 완료 상태를 보여준다. 급여는 초안 이후 근태 변경이 확인된 경우에만 재검토 항목에 넣는다.
- 기존 운영 요약은 같은 범위의 재조회가 실패해도 직전 정상 응답을 지웠다. 이번 변경으로 같은 사업장·범위·필터 안에서는 마지막 정상 조회분을 유지하고, 로딩·오류 문구로 오래된 결과임을 알린다. 사업장이나 필터가 바뀌면 이전 결과를 재사용하지 않는다.

## 검증

- `npm test`: 59개 통과, 실패 0개.
- `npm run build`: 통과. 기존 CSS autoprefixer 경고는 남아 있다.
- `git diff --check`: 통과.

## 남은 검증과 설계 차이

- 이번 변경의 실제 로그인 브라우저 E2E와 운영 DB 쓰기 테스트는 수행하지 않았다. 배포·운영 DB 적용도 하지 않았다.
- 급여 초안 재검토 판단은 현재 근태 `updated_at`만 대조한다. 설계 문서가 언급한 일정·계약·설정·조정 변경까지 판단하려면 각 데이터의 영향 월과 변경 시각을 정의한 후 서버 집계를 확장해야 한다. 지금 문구는 근태 변경만 근거로 한다.
- 홈의 확인 항목은 현재 월 1일부터 어제까지다. 월 경계를 넘는 과거 미정정 기록을 포함하려면 별도의 누적 미해결 조회 범위를 정해야 한다.
- `docs/101`의 브라우저·격리 DB 결과는 2026-09-17 당시 코드 검증 이력이다. 이번 변경의 실제 브라우저 재검증 결과로 대체해서는 안 된다.
