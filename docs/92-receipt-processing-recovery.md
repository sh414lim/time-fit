# 영수증 OCR 재처리·복구 흐름

- 관리자 검토함은 `검토 필요`와 `OCR 실패` 영수증을 함께 조회한다.
- 실패 영수증에 원본 보기, 실패 원인, 다시 분석 동선을 제공한다.
- 원본 파일은 재업로드하지 않고 기존 비공개 Storage 객체를 다시 사용한다.
- 동시에 처리 중인 문서의 중복 실행을 차단한다.
- 자동 재처리는 문서당 최대 5회로 제한해 Vision/LLM 비용 폭증을 방지한다.
- 이미 지출 원장에 반영된 영수증은 재처리하지 않는다.
